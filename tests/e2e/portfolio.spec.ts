import { test } from "@playwright/test";

// E2E tests require the Next.js app running at localhost:3000.
// These are stubs until the app is deployed/running in CI.

test.skip("navigate to /portfolio, verify three spread views render", async () => {});
test.skip("account filter on /portfolio updates chart data", async () => {});
test.skip("incomplete holdings warning banner shown when incompleteHoldings > 0", async () => {});
test.skip("all-NOT_FOUND portfolio shows no enrichment data message", async () => {});
