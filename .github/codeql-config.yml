# CodeQL configuration file

# Define which paths to exclude from the CodeQL analysis
excluded-paths:
  - tests/

# Use the security-extended query pack
queries:
  - security-extended

# Exclude experimental queries
experimental: false
